<UserControl x:Class="EOkno.Views.PositionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:v="clr-namespace:EOkno.Views"
             xmlns:shared="clr-namespace:Okna.Plugins"
             Background="{StaticResource {x:Static SystemColors.ControlLightLightBrushKey}}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <UserControl.Resources>
    <shared:BoolNegateConverter x:Key="BoolNegateConverter"/>
  </UserControl.Resources>
  
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <CheckBox x:Name="chkInheritDoc" 
              Margin="6"
              >
      Podle nastavení dokumentu
    </CheckBox>

    <v:ColorsAndComponentsView Grid.Row="1"
                               IsEnabled="{Binding IsChecked, ElementName=chkInheritDoc, Converter={StaticResource BoolNegateConverter}}"
                               />
  </Grid>
</UserControl>
